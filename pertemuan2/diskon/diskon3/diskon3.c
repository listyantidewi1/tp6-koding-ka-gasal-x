#include <stdio.h>

int main() {
    int harga1 = 25000, harga2 = 15000, harga3 = 14000;
    int total = harga1 + harga2 + harga3 - 10000;
    printf("Total setelah diskon tetap: %d\n", total);
    return 0;
}